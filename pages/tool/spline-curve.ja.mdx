import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon'
import { Callout } from 'nextra/components'
import Kst from '/components/KeyboardShortcutsTitle'

## Spline Curve

{/* <Reference shortcut="" command="Spline Curve" button="Toolbar"  buttonIcon='button-spline-curve'/> */}

<CommandReference
  command="Spline Curve"
  shortcut=""
  button="ツールバー内"
  buttonIcon="/img/button-spline-curve.png"
  tier="both"
/>

## 説明

クリックしたポイントを通過するカーブを作成できます。

## 基本的な使い方

1. ツールバーの <ButtonIcon type="button-spline-curve" /> ボタンをクリックします。
2. カーブの開始点をクリックします。
3. カーブが通過するポイントをクリックしてカーブを作成します。
   - <Kst>キーボードショートカット</Kst>
      - <kbd>K</kbd> **Knife:** [ナイフモードの使用](/tool/sketching-essentials#use-knife-mode) を参照。
      - <kbd>Tab</kbd> **コントロール / 通過点:** コントロールポイントカーブに切り替えます。
      - <kbd>Ctrl + Z</kbd> **元に戻す:** 最後のクリックを取り消します。
      - <kbd>X</kbd>/<kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z 軸:** [軸に制限する](/tool/sketching-essentials#constrain-to-an-axis)。
4. 最後のポイントをクリックしたら、右クリックで確定します。
5. カーブが作成されます。

<Callout type="info">
**2 種類のカーブの違い**  
   - **Spline Curve:** カーブはクリックしたすべての点を通過します。  
   - **Control Point Curve:** クリックした点はコントロールポイントとなり、カーブ上に位置しない場合があります。
</Callout>

<VideoPlayer fileName="spline-and-control-point-curve"/>
