import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import ButtonIcon from '/components/ButtonIcon'

{/* # Rebuild

<Reference shortcut="" command="Rebuild" button="Im vorgeschlagenen Befehlsmenü" buttonIcon='button-rebuild'/> */}

Der Rebuild-Befehl wird basierend auf dem ausgewählten Objekt ausgeführt:
   - **Rebuild-Kurve:** Wenn Kurven ausgewählt sind, siehe [**Rebuild-Kurve**](/sketch/rebuild-curve).
   - **Rebuild-Fläche:** Wenn Flächen ausgewählt sind, siehe [**Rebuild-Fläche**](/solid/rebuild-face) im Abschnitt „Solid-Befehle“.

## See also
- [Rebuild Curve](rebuild-curve)
- [Rebuild Face](/solid/rebuild-face)
- [Delete Redundant Topology](delete-redundant-topology)