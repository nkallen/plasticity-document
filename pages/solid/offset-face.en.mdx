import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'

## Offset Face

{/* <Reference shortcut="" command="Push Face" button="" /> */}

<CommandReference
  command="Push Face"
  shortcut=""
  button="In Command Palette"
  buttonIcon="/img/button-push-face.png"
  tier="both"
/>

## Description

Offset the selected Faces by specifying the distance. Optional adjacent angle specification available.

## Basic Usage

1. Select the Faces you want to offset.
2. When a Face is selected, the default command, **Push Face**, is automatically executed.
3. Move the yellow dot to specify the distance.
4. Set the options in the Command Dialog and Keyboard Shortcuts.
     - <Kst>Command Dialog</Kst>
          - **Distance:** Specify the distance.
          - **Angle Adjacent:** Specify the Angle.
          - **Grow:**
               - When the concave inner Face is offset.
               - **Moving:** Move the whole wall when the offset gets to the outer wall.
               - **Fixed:** Stop at the outer wall.
               - **None:** Keep going by itself neither affecting or affected by the outer wall.
     - <Kst>Keyboard Shortcuts</Kst>
          - <kbd>D</kbd> **Distance** Specify the distance by moving the cursor.
          - <kbd>A</kbd> **Angle adjacent** Specify the angle by moving the cursor.
          - <kbd>Tab</kbd> **Grow mode** Switches the grow mode.
5. Confirm with OK in the Command Dialog or right-click.

<VideoPlayer fileName="offset-face"/>

## Dependant Offset

Alternate the distance and surface area of Faces to match and intersect with another Face.

1. Select the Faces you want to offset.
2. When a Face is selected, the default command, **Push Face**, is automatically executed.
3. When offsetting the Face, select a secondary face you want to match with.
4. Confirm with OK in the Command Dialog or right-click. 

<VideoPlayer fileName="dependant-offset-face"/>

## See also
- [Draft Face](draft-face)
- [Match Face](match-face)
- [Delete Face](delete-face)