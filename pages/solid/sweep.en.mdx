import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import CommandReference from '/components/CommandReference'

## Sweep

{/* <Reference shortcut="Shift + P" command="Sweep" button="In Suggested Commands Menu"  buttonIcon="button-sweep"/> */}

<CommandReference
  command="Sweep"
  shortcut="Shift + P"
  button="In Suggested Commands Menu"
  buttonIcon="/img/button-sweep.png"
  tier="both"
/>

## Description

Sweeping Faces or Regions generates Solids, while sweeping Curves produces Sheets. Specify twisting angle in degrees and optionally select multiple sweep guides. It now supports boolean operations.

## Basic Usage

1. Select the Regions, Faces, or Curves to be swept.
2. Press <kbd>Shift + P</kbd>.
3. Select the Curve that will serve as the path for the sweep.
4. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - **Twist:** Specify the twisting angle in degrees.
      - **Scale:** Specify the scale value on the ending point of the pipe. Click on the option dialog and manually enter your value or drag with your mouse from left to right.
      - **Alignment:** 
         - **Parallel:** Sweeps parallel to the path, regardless of the orientation. 
         - **Normal:** Sweeps at an angle aligned with the path.
      - **Distance:**
         - Adjust the result length. Decreasing the value from 1.00 to 0 shortens the length. (Default: 1.00)
   - <Kst>Advanced Dialog</Kst>
      - **Keep tools:** Keep the swept body after a boolean operation.
      - **Simplify:** Specify whether to generate trimmed planar sections or maintain uniform geometry. (Default: OFF)
      - **Corner type:**
         - Mitre: Have an external pointy corner.
         - Round: Have an external round corner.
   - <Kst>Keyboard Shortcuts</Kst>
      - <kbd>D</kbd> **Twist:** Specify the Twist with mouse movement.
      - <kbd>Shift + Q</kbd> **Slice**, <kbd>Q</kbd> **Union**, <kbd>W</kbd> **Difference**, <kbd>Shift + E</kbd> **Intersect:** Perform a boolean operation.
      - <kbd>B</kbd> **New body:** Retain the resulting body after a boolean operation or modeling step.
      - <kbd>T</kbd> **Keep tools:** Performs Boolean operations while keeping the tool objects intact.
      - <kbd>Ctrl</kbd> **Snap:** Constrains snapping to fixed 5° degree steps.
5. Optionally select sweep guides.
      1. Make sure that **Select guides:** is highlighted in the Command Dialog.
      2. Select the Curves that will serve as guides for the sweep.
         - Use <kbd>Shift</kbd> + click to select multiple guides.
         - Use <kbd>Ctrl</kbd> + click to deselect.
         - The order in which you select the guides will affect the shape of the sweep.
      3. Set the options in the Command Dialog
         - <Kst>Command Dialog</Kst>
            - **Guide method:**
               - **Point:** The cross-section of the swept body is rotated and scaled so that the points initially coincident with the guide and path always remain in contact.
               - **Chord:**  The cross-section is rotated but not scaled.
               - **Curve:** The cross-section is rotated but not scaled. The guide and path must touch the profile. The profile stays in contact with both guide wires, but only one of the contact points is at a fixed
               point on the profile; the other is free to move along the profile.
               Note: The profile must be large enough to span the greatest distance between the two guide wires.
6. Confirm with OK in the Command Dialog or right-click.
7. The object created depends on the initial selection.
   - If you select Regions and Faces, a Solid is created.
   - If you select a  Curve, a Sheet is created.

<VideoPlayer fileName="sweep"/>

## See also
- [Pipe](pipe)