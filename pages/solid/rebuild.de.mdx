import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import ButtonIcon from '/components/ButtonIcon'

## Rebuild

{/* <Reference shortcut="" command="Rebuild" button="Im Menü „Vorgeschlagene Befehle“" buttonIcon="button-rebuild"/> */}

Der **Rebuild**‑Befehl wird basierend auf dem ausgewählten Objekt ausgeführt.
  - Wenn Kurven ausgewählt sind: siehe [**Rebuild Curve**](/sketch/rebuild-curve).
  - Wenn Flächen ausgewählt sind: siehe [**Rebuild Face**](/solid/rebuild-face).
