import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import ButtonIcon from '/components/ButtonIcon'

## Rebuild

{/* <Reference shortcut="" command="Rebuild" button="In Suggested Commands Menu" buttonIcon="button-rebuild"/> */}

The **Rebuild** command is run based on the selected object.
  - When Curves are selected: see [**Rebuild Curve**](/sketch/rebuild-curve).
  - When Faces are selected: see [**Rebuild Face**](/solid/rebuild-face).