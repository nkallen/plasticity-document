import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon'
import Kst from '/components/KeyboardShortcutsTitle'

## Scale

{/* <Reference shortcut="S" command="Scale" button="In Command Bar" buttonIcon='button-scale' /> */}

<CommandReference
  command="Scale"
  shortcut="S"
  button="In Command Bar"
  buttonIcon="/img/button-scale.png"
  tier="both"
/>

## Description

Scale objects using Scale Gizmo. Options for single-axis constraints or [**freestyle scale**](#freestyle-scale) are available. The pivot point is also adjustable. When you insert a new primitive or sketch object at its initial insertion point, the Scale Gizmo and pivot automatically re-orient to align with the object's surface normal.

## Basic Usage

1. Select the objects you want to scale.
2. Press <kbd>S</kbd>.
3. Use the displayed Scale Gizmo to resize.
4. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - **XYZ:** Specify the scale of the X, Y, and Z axes.
      - **Orientation:** Set the orientation of the scale gizmo.
         - <ButtonIcon type="button-normal"/> **Normal:** Sets the orientation to align with the object's surface normal.
         - <ButtonIcon type="button-pivot"/> **Pivot:** Sets the orientation to match the previously defined pivot orientation.
         - <ButtonIcon type="button-construction-plane"/> **Construction Plane:** Sets the orientation to align with the active construction plane.
         - <ButtonIcon type="button-world"/> **World:** Sets the orientation to align with the global (world) axis.
      - **Pivot:** Set the position of the pivot point.
         - <ButtonIcon type="button-bbox"/> **Bbox:** Positions the pivot at the center of the bounding box enclosing the selected objects.
         - <ButtonIcon type="button-median"/> **Median:** Positions the pivot at the average location of the pivots of the selected objects.
         - <ButtonIcon type="button-active"/> **Active:** Positions the pivot at the last selected (active) object's pivot.

         ![Pivot Demonstration](/img/pivot-scale.png)
      - Advanced:
         - **Transform instances inversely**: Scales only the parent object while keeping any attached instances in place, by automatically applying the inverse transformation to each instance.
   - <Kst>Keyboard Shortcuts</Kst>
   - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [Constrain to the X, Y, or Z-axis](/tool/sketching-essentials#constrain-to-an-axis).
   - <kbd>Shift + X</kbd>/ <kbd>Shift + Y</kbd>/<kbd>Shift + Z</kbd> **X, Y, Z plane:** Constrain to the X, Y, or Z-plane.
   - <kbd>S</kbd> **Uniform:** Scales uniformly in the X, Y, and Z directions.
   - <kbd>F</kbd> **Freestyle:** Specify the scaling axis by clicking on the starting and ending points, and then click with the desired scaling ratio.
   - <kbd>V</kbd> **Pivot:** Click to specify the position of the Gizmo.
   - <kbd>W</kbd> **Worldspace:** Change the Gizmo's coordinate axis to world coordinates.
4. Confirm with OK in the Command Dialog or right-click. 

## Freestyle Scale[#freestyle-scale]

This command is equivalent to the <kbd>F</kbd> option of the **Scale** command.

1. Select the objects you want to scale.
2. Use the **Freestyle Scale** command in the Command Palette.
3. Specify the scaling axis by clicking on the starting and ending points.
4. Move the cursor to scale the object, and then click the desired scaling ratio.
5. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - **Ratio:** Specify the ratio of the scaled object.
      - **Length:** Specify the length of the scaled object.
      - **Uniform:** Uniformly scale the object, keeping proportions intact.
6. Confirm with OK in the Command Dialog or right-click.

<VideoPlayer fileName="freestyle-scale"/>

## See also
- [Mirror](mirror)
- [Move](move)
- [Rotate](rotate)