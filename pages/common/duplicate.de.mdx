import Reference from '/components/Reference'
import { Callout } from 'nextra/components'
import VideoPlayer from '/components/VideoPlayer';
import Kst from '/components/KeyboardShortcutsTitle'

## Duplizieren

{/* <Reference shortcut="Shift + D" command="Duplizieren" button="In Command Bar" buttonIcon='button-duplicate' /> */}

<CommandReference
  command="Duplizieren"
  shortcut="Shift + D"
  button="In Command Bar"
  buttonIcon="/img/button-duplicate.png"
  tier="both"
/>

## Beschreibung

Objekte duplizieren und mit dem Verschieben-Gizmo bewegen. Einzelachsenbeschränkungen oder Freihand-Bewegung sind möglich. Der Pivot ist einstellbar.

<Callout type="info">
**Hinweis:** Beim Duplizieren mehrerer Regionen werden diese als kombinierte Form dupliziert.
</Callout>

## Grundlegende Verwendung

1. Wählen Sie die Objekte aus, die Sie duplizieren möchten.
2. Drücken Sie <kbd>Shift + D</kbd>.
3. Verwenden Sie den angezeigten Verschieben-Gizmo zum Bewegen.
4. Legen Sie die Optionen im Befehlsdialog und den Tastenkombinationen fest.
   - <Kst>Befehlsdialog</Kst>
      - **XYZ:** Geben Sie den Verschiebebetrag der X-, Y- und Z-Achse an.
   - <Kst>Tastenkombinationen</Kst>
      - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-Achse:** [Auf die X-, Y- oder Z-Achse beschränken](/tool/sketching-essentials#constrain-to-an-axis).
      - <kbd>Shift + X</kbd>/ <kbd>Shift + Y</kbd>/<kbd>Shift + Z</kbd> **X-, Y-, Z-Ebene:** Auf die X-, Y- oder Z-Ebene beschränken.
      - <kbd>G</kbd> **Bildschirmraum:** Auf die sichtbare Ebene beschränken.
      - <kbd>F</kbd> **Freihand:** Klicken, um den Start- und Endpunkt anzugeben, der Richtung und Abstand der Bewegung bestimmt.
      - <kbd>V</kbd> **Pivot:** Klicken, um die Position des Gizmos anzugeben.
      - <kbd>W</kbd> **Weltkoordinaten:** Die Koordinatenachse des Gizmos auf Weltkoordinaten ändern.
4. Mit OK im Befehlsdialog oder Rechtsklick bestätigen.

<VideoPlayer fileName="duplicate"/>

## Siehe auch
- [Platzieren](place)