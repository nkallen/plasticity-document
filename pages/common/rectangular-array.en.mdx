import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon';
import Kst from '/components/KeyboardShortcutsTitle'
import { Callout } from 'nextra/components'

## Rectangular Array

{/* <Reference shortcut="" command="Rectangular Array" button="In Suggested Commands Menu"  buttonIcon='button-rectangular-array'/> */}

<CommandReference
  command="Rectangular Array"
  shortcut=""
  button="In Command Bar"
  buttonIcon="/img/button-extend-curve.png"
  tier="both"
/>

## Description

Duplicate objects in a linear pattern by specifying the number of duplicates. Optionally, define another direction to create a rectangular array. Option to create instances is possible.

<Callout type='info'>
**Note:** Images and meshes (.OBJ, .STL, .FBX, etc.) can be arrayed, with controls for duplication direction, number of copies, and duplication modes. Arraying instance objects is also supported.
</Callout>

## Basic Usage

1. Select the object you want to duplicate.
2. Click the button <ButtonIcon type="button-rectangular-array" /> on the Right of Command Bar.
3. The duplication preview will be displayed.
   - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [Constrain to the X, Y, or Z-axis](/tool/sketching-essentials#constrain-to-an-axis).
4. Click on the desired direction.
5. Set the options in the Command Dialog and Keyboard Shortcuts.
      - **Mode:**
         - **Extent:** Specify the extent distance at which all duplications will be evenly arranged in. 
         - **Spacing:** Specify the spacing distance for each of the duplications.
   - <Kst>Command Dialog</Kst>
      - **Distance 1:** Specify the distance.
      - **Number:** Specify the number of duplications.
      - **Make instances:** Create instances instead of independent copies. (See [Create Instances](/solid/create-instance))
   - <Kst>Keyboard Shortcuts</Kst>
   - <kbd>Shift + Wheel up</kbd> **Add copy:** Increases the number of duplications.
   - <kbd>Shift + Wheel down</kbd> **Subtract copy:** Reduces the number of duplications.
6. If you want to duplicate in another direction, click on **Select endpoint 2** in the dialog.
   1. Right-click on the desired direction for duplication.
   2. Set the options in the Command Dialog 
      - <Kst>Command Dialog</Kst>
         - **Distance 2:** Specify the distance.
         - **Repeat:** Specify the number of repetitions.
         - **Make instances:** Create instances instead of independent copies. (See [Create Instances](/solid/create-instance))
      - <Kst>Keyboard Shortcuts</Kst>
      - <kbd>Shift + Wheel up</kbd> **Add copy:** Increases the number of duplications.
      - <kbd>Shift + Wheel down</kbd> **Subtract copy:** Reduces the number of duplications.
      - <kbd>I</kbd> **Toggle make instances:** Toggles whether the mirrored half is created as an instance or an independent copy.
7. Confirm with OK in the Command Dialog or right-click.

<VideoPlayer fileName="rectangular-array"/>

## See also
- [Curve Array](curve-array)
- [Radial Array](radial-array)