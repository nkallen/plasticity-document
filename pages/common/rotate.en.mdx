import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon'
import Kst from '/components/KeyboardShortcutsTitle'

## Rotate

{/* <Reference shortcut="R" command="Rotate" button="In Command Bar"  buttonIcon='button-rotate'/> */}

<CommandReference
  command="Rotate"
  shortcut="R"
  button="In Command Bar"
  buttonIcon="/img/button-rotate.png"
  tier="both"
/>

## Description

Rotate objects using Rotate Gizmo. Options for single-axis constraints or [**freestyle rotation**](#freestyle-rotate) are available. The pivot point is also adjustable. When you insert a new primitive or sketch object at its initial insertion point, the Rotate Gizmo and pivot automatically re-orient to align with the object's surface normal.

## Basic Usage

1. Select the objects you want to rotate.
2. Press <kbd>R</kbd>.
3. Use the displayed Rotation Gizmo to rotate.
4. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - **Angle:** Specify the last specified angle using the rotation Gizmo as a numerical value
      - **Axis:** Specify the rotation axis using the X, Y, or Z vector.
      - **Orientation:** Set the orientation of the rotate gizmo.
         - <ButtonIcon type="button-normal"/> **Normal:** Sets the orientation to align with the object's surface normal.
         - <ButtonIcon type="button-pivot"/> **Pivot:** Sets the orientation to match the previously defined pivot orientation.
         - <ButtonIcon type="button-construction-plane"/> **Construction Plane:** Sets the orientation to align with the active construction plane.
         - <ButtonIcon type="button-world"/> **World:** Sets the orientation to align with the global (world) axis.
      - **Pivot:** Set the position of the pivot point.
         - <ButtonIcon type="button-bbox"/> **Bbox:** Positions the pivot at the center of the bounding box enclosing the selected objects.
         - <ButtonIcon type="button-median"/> **Median:** Positions the pivot at the average location of the pivots of the selected objects.
         - <ButtonIcon type="button-active"/> **Active:** Positions the pivot at the last selected (active) object's pivot.

         ![Pivot Demonstration](/img/pivot-rotate.png)
      - Advanced:
         - **Transform instances inversely**: Rotates only the parent object while keeping any attached instances in place, by automatically applying the inverse transformation to each instance.
   - <Kst>Keyboard Shortcuts</Kst>
   - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [Constrain to the X, Y, or Z-axis](/tool/sketching-essentials#constrain-to-an-axis).
   - <kbd>G</kbd> **Screen space:** Limit to the visible plane.
   - <kbd>F</kbd> **Freestyle:** Click the starting point and endpoint of the rotation axis, and then click the reference point. Move the cursor to rotate the object, and then click to set the desired angle.
   - <kbd>V</kbd> **Pivot:** Click to specify the position of the Gizmo.
   - <kbd>W</kbd> **Worldspace:** Change the Gizmo's coordinate axis to world coordinates.
5. Confirm with OK in the Command Dialog or right-click.

## Freestyle Rotate [#freestyle-rotate]

This command is equivalent to the <kbd>F</kbd> option of the **Rotate** command.

1. Select the objects you want to rotate.
2. Use the **Freestyle Rotate**  command in the Command Palette.
3. Click the starting point and endpoint of the rotation axis, and then click the reference point.
4. Move the cursor to rotate the object, and then click to set the desired angle. 
5. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - Same options as above in [Rotate](/common/rotate#basic-usage).
   - <Kst>Keyboard Shortcuts</Kst>
      - Same options as above in [Rotate](/common/rotate#basic-usage).
6. Confirm with OK in the Command Dialog or right-click.

<VideoPlayer fileName="freestyle-rotate"/>

## See also
- [Mirror](mirror)
- [Move](move)
- [Scale](scale)